version: "3.9"

services:
  frontend:
    build:
      context: ./frontend
    container_name: frontend-service
    ports:
      - "3000:3000"

  backend:
    build:
      context: ./backend
    container_name: backend-service
    ports:
      - "5000:5000"
