apiVersion: v1
kind: Service
metadata:
  name: {{ include "parallax-app.fullname" . }}
  labels:
    {{- include "parallax-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "parallax-app.selectorLabels" . | nindent 4 }}
